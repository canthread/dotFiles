server {
    server_name radarr.tekebai.com;  # Replace with your domain

    location / {
        proxy_pass http://192.168.1.29:7878;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support (for SignalR)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_buffering off;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/radarr.tekebai.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/radarr.tekebai.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = radarr.tekebai.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name radarr.tekebai.com;
    return 404; # managed by Certbot


}